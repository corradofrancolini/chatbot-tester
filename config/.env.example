# =============================================================================
# CHATBOT TESTER - Credenziali
# =============================================================================
# Copia questo file in .env e compila i valori necessari.
# ATTENZIONE: NON committare mai il file .env!
# =============================================================================

# -----------------------------------------------------------------------------
# LangSmith (opzionale - per tracking e debugging avanzato)
# -----------------------------------------------------------------------------
# Ottieni la API Key da: https://smith.langchain.com > Settings > API Keys
LANGSMITH_API_KEY=

# -----------------------------------------------------------------------------
# Google OAuth (opzionale - per report su Google Sheets)
# -----------------------------------------------------------------------------
# Segui la guida: docs/GOOGLE_SETUP.md per creare le credenziali OAuth
# Indica il path al file JSON delle credenziali OAuth
GOOGLE_OAUTH_CREDENTIALS=./config/oauth_credentials.json
