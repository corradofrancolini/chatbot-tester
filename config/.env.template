# .env.template - Template per variabili d'ambiente
# QUESTO FILE PUÒ ESSERE COMMITTATO (non contiene secrets)
#
# Setup:
#   1. Copia questo file: cp config/.env.template config/.env
#   2. Sostituisci i placeholder con le tue API keys reali
#   3. Il file .env è in .gitignore e NON deve essere committato
#
# Le variabili vengono caricate automaticamente da src/config_loader.py

# LangSmith - Tracing e osservabilità
LANGSMITH_API_KEY=your_langsmith_api_key_here  # pragma: allowlist secret

# Anthropic - Claude API
ANTHROPIC_API_KEY=your_anthropic_api_key_here  # pragma: allowlist secret

# OpenAI - GPT models (se usato per evaluation)
OPENAI_API_KEY=your_openai_api_key_here  # pragma: allowlist secret

# Google API - Per Google Sheets integration
GOOGLE_API_KEY=your_google_api_key_here  # pragma: allowlist secret

# CircleCI - Per trigger automatici da MCP
CIRCLECI_TOKEN=your_circleci_token_here  # pragma: allowlist secret
